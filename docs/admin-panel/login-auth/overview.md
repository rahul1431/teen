# Login/Auth — Overview

Username/password + optional TOTP 2FA login, JWT session (8h expiry), and the 2FA setup/verify/disable flow (surfaced on the Profile page, not here — see `docs/admin-panel/profile-settings/`).
